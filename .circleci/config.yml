version: 2
jobs:
  build:
    branches:
        only:
            - develop
    docker:
      - image: circleci/node:lts
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install
      - run:
          name: Run Karma Tests
          command: npm test
